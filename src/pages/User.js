import React from 'react';
import userProfile from '../components/userProfile'

function User(props) {
    return (
        <div>
            <userProfile />
        </div>
    );
}

export default User;